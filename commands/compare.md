---
description: Compare two options on fixed axes
allowed-tools: WebSearch, WebFetch, Read, Grep, Glob
---

Options to compare: $ARGUMENTS

Compare the two options on these axes, in this order:
correctness and reliability, performance, maintenance burden,
adoption cost in this specific codebase, and ecosystem maturity.

Rules:
- Do not ask clarifying questions.
- Inspect this codebase to ground the adoption cost axis in real files.
- Produce a table with one row per axis and one column per option.
- After the table, give a recommendation in two sentences, including
  the single condition under which the other option wins instead.
- Do not hedge into a tie. Pick one.

