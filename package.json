{
  "name": "agent-commands",
  "private": true,
  "scripts": {
    "sync": "mkdir -p $HOME/.claude/commands $HOME/.config/opencode/commands && cp commands/*.md $HOME/.claude/commands/ && cp commands/*.md $HOME/.config/opencode/commands/",
    "prepare": "npm run sync"
  }
}
